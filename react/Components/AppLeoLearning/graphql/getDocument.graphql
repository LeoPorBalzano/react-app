query getDocument(
  $acronym: String
  $fields: [String]
  $where: String
  $sort: String
) {
  documents(acronym: $acronym, fields: $fields, where: $where, sort: $sort) {
    fields {
      value
    }
  }
}
